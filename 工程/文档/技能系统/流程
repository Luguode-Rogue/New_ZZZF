# 开发流程指南

## 1. 遇到新类时的研究流程

当遇到一个不熟悉的新类（如 `ItemObject` 类）时，按以下步骤研究：

### 步骤1：查看官方API文档
- **URL**：https://apidoc.bannerlord.com/v/1.2.9/
- **目的**：简单了解该类有哪些属性、方法、事件
- **示例**：查看 `ItemObject` 类有哪些方法可以获取武器伤害、速度等

### 步骤2：使用dnSpy反编译查看
- **工具**：dnSpy（.NET反编译器）
- **目的**：
  - 了解如何创建该类物品
  - 了解如何调用该类物品
  - 查看内部实现逻辑

### 步骤3：特殊类型的入手点
- **反序列化方法**：很多游戏对象的创建都是通过反序列化完成的
- **示例**：`ItemObject` 的创建可能通过 `ItemObject.Deserialize()` 方法
- **查找方法**：在dnSpy中搜索 `ReadFromXML` 或 `Deserialize` 相关方法

---

## 2. 技能开发流程

### 步骤1：设计技能
- 确定技能类型（主主动/副主动/被动/法术/战技）
- 确定触发条件（按键/自动触发/受击触发）
- 确定资源消耗（耐力/法力）
- 确定冷却时间
- 确定效果范围（自身/单体/群体/方向性范围）

### 步骤2：创建技能类
- 在 `Skills` 目录下新建类文件
- 继承 `SkillBase` 基类
- 实现构造函数（设置SkillID、Type、Cooldown、ResourceCost）
- 实现 `Activate()` 方法（技能核心逻辑）
- 实现 `CheckCondition()` 方法（AI触发条件）

### 步骤3：注册技能
- 在 `SkillFactory.cs` 的 `_skillRegistry` 中添加条目
- 格式：`{ "SkillID", new SkillClass() }`

### 步骤4：配置兵种XML
- 编辑 `ModuleData/troop_skills.xml`
- 将技能分配到对应槽位（MainActive/SubActive/Passive/Spells/CombatArts）

### 步骤5：测试验证
- 基础验证：日志输出、资源扣除、冷却生效
- 进阶验证：边界情况、多人游戏同步

---

## 3. Buff/Debuff开发流程

### 步骤1：创建状态类
- 继承 `AgentBuff` 基类
- 实现构造函数（设置StateId、Duration）
- 实现 `OnApply()` 方法（Buff生效时触发）
- 实现 `OnUpdate()` 方法（每帧更新）
- 实现 `OnRemove()` 方法（Buff移除时触发）

### 步骤2：在技能中应用Buff
- 在技能的 `Activate()` 方法中创建Buff实例
- 添加到 `AgentSkillComponent.StateContainer`

### 步骤3：修改属性计算（如需要）
- 如果Buff需要修改Agent属性（速度、伤害等）
- 在自定义的 `AgentStatCalculateModel` 中添加逻辑
- 根据 `StateContainer.HasState("BuffID")` 来判断

### 步骤4：修改伤害计算（如需要）
- 如果Buff需要修改伤害
- 在自定义的 `DamageModel` 中添加逻辑
- 根据Buff状态来调整伤害倍率

---

## 4. 调试流程

### 步骤1：日志输出
- 使用 `MBInformationManager.AddQuickInformation()` 输出调试信息
- 在关键位置添加日志，跟踪执行流程

### 步骤2：使用调试命令
- **Ctrl+F5**：给予随机词缀武器
- **Ctrl+F6**：给予随机词缀护甲
- **Ctrl+F7**：列出玩家词缀物品
- **Ctrl+F8**：打印系统状态
- **Ctrl+F9**：重roll随机词缀
- **F12**：切换Debug模式（解锁所有技能和兵种模板）

### 步骤3：查看游戏日志
- 游戏日志位置：`Documents\Mount and Blade II Bannerlord\Logs\`
- 查看错误信息和异常堆栈

---

## 5. 代码审查流程

### 步骤1：检查性能
- 避免在 `OnUpdate()` 中做耗时操作
- 使用缓存机制（如技能检测结果缓存）
- 使用对象池（如投射物对象管理）

### 步骤2：检查线程安全
- 如果在 `MissionTick` 中更新状态，需通过副本遍历
- 避免 `InvalidOperationException`

### 步骤3：检查内存泄漏
- 确保Buff移除时清理所有引用
- 确保事件订阅在适当时候取消订阅

---

## 6. 文档编写流程

### 步骤1：编写技能说明
- 技能名称、类型、消耗、冷却
- 技能效果描述
- 实现要点和注意事项

### 步骤2：编写代码示例
- 提供关键代码段的示例
- 添加注释说明

### 步骤3：编写测试验证
- 列出测试场景和预期结果
- 提供调试方法

---

## 7. 版本管理流程

### 步骤1：维护更新日志
- 记录新增功能、修复的Bug
- 示例：
  ```
  ## v1.1.0 - 2026-06-15
  - 新增技能: 战意（主主动）
  - 修复: 威压技能范围检测错误
  ```

### 步骤2：打包Mod
- 确保 `bin/` 目录包含所有编译后的DLL
- 验证 `SubModule.xml` 依赖项正确
- 测试安装和卸载

---

## 总结

开发流程的核心原则：
1. **研究先行**：遇到新类先查API和反编译
2. **分步实现**：按设计→实现→配置→测试的流程
3. **持续调试**：使用日志和调试命令
4. **代码审查**：检查性能、线程安全、内存泄漏
5. **文档跟进**：及时编写和更新文档
6. **版本管理**：维护更新日志和打包