# 2026-06-09 清理旧弹道显示代码

## 任务描述
弹道显示功能已独立为 `ProjectileTrajectorySystem` 模块，从 New_ZZZF 项目中删除旧的弹道显示代码，同时确保不影响其他功能（技能制导追踪、自瞄、缩放瞄准环等）。

## 修改的文件列表

### 1. 删除的文件
| 文件 | 说明 |
|------|------|
| `工程/New_ZZZF/Systems/ProjectileTrajectorySystem.cs` | 旧的弹道显示系统（388行），已被独立模块替代 |

### 2. 修改的文件：`SkillSystemBehavior.cs`
| 改动 | 内容 |
|------|------|
| 移除 `WoW_Line` 字典声明 | 仅被旧弹道显示和死代码使用 |
| 移除 `OnMissionTick` 中旧弹道调用块 | `ProjectileTrajectorySystem.UpdateTrajectoryRangeWeapon` + `ProjectileTrajectorySystem.UpdateTrajectory` 及配套攻城器检测逻辑 |
| 移除 `OnAfterMissionCreated` 中 `WoW_Line.Clear()` | 配套清理 |
| 移除 `OnEndMission` 中 `WoW_Line.Clear()` | 配套清理 |

### 3. 修改的文件：`Script.cs`
| 改动 | 内容 |
|------|------|
| 移除 `missionWeapondandaoxianshi()` 方法 | 死代码，从未被调用 |
| 移除 `CalculatePositionAtTime()` 方法 | 仅被上述死代码调用，且引用了已删除的 `WoW_Line` |

## 保留的共享组件（未受影响）
| 组件 | 原因 |
|------|------|
| `ProjectileData.cs` | 被7+个技能文件用于制导追踪系统 |
| `AgentMissileSpeedData.cs` | 被 Script.cs 自瞄系统使用 |
| `WoW_CustomGameEntity` | 技能制导系统 + Script.cs 缩放环共享 |
| `WoW_ProjectileDB` | 投射物制导追踪核心字典 |
| `WoW_Ring` | 缩放瞄准环功能 |
| `WoW_AgentMissileSpeedData` | 自瞄系统核心数据 |
| `UpdateProjectileTargets()` | 缩放键瞄准环（GameKey 24），独立功能 |

## 验证结果
- `ProjectileTrajectorySystem` 引用：**0个残留**
- `WoW_Line` 引用：**0个残留**
- `missionWeapondandaoxianshi` / `CalculatePositionAtTime` 引用：**0个残留**
- 共享数据（`WoW_CustomGameEntity`、`WoW_ProjectileDB`、`WoW_Ring`、`WoW_AgentMissileSpeedData`）：在9个文件中正常使用，未受影响
