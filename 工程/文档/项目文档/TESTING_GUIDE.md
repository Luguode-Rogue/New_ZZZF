# New_ZZZF 测试指南

## 通用测试

每个功能至少验证首次初始化、重复打开/关闭、Mission 切换、Mission 结束、重复进入、异常输入和存档读档。

## UI 测试

重点检查焦点、鼠标命中、键盘快捷键、层级遮挡、窗口显隐、输入模式以及 WebView2/Gauntlet 生命周期。

## 兼容测试

涉及 Bannerlord API 的功能必须验证目标支持版本，并记录实际使用的字段、方法和 fallback。

## Bug 回归

已修复问题必须加入回归检查。原始 Bug 记录和失败方案不可删除，避免后续修复重新走已经证伪的路线。
